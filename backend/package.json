{
    "name": "backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "dev": "nodemon --watch . --ext js,prisma --exec \"npm run prisma:generate && node src/index.js\"",
        "start": "node src/index.js",
        "prisma:generate": "prisma generate",
        "studio": "prisma studio"
    },
    "dependencies": {
        "@prisma/client": "^4.15.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.0",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "nodemon": "^2.0.22",
        "prisma": "^4.15.0"
    }
}
